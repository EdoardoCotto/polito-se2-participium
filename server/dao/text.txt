The db interactions have to be only in this folder, we must not write SQL code out of this folder and we will write a different file
for every object of the db
example: all the informations that we need related to the user should be in the file UserDAO.js and we should do that for every object